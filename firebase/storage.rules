rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow any depth under users/{uid}/...
    match /users/{uid}/{allPaths=**} {
      allow write: if request.auth != null && request.auth.uid == uid;
      allow read: if false; // clients do not read raw videos
    }
  }
}


